/*
 * Wallet Messages
 *
 * This contains all the text for the Wallet container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Wallet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Wallet container!',
  },
});
